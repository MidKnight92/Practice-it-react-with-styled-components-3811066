import React from "react";

// const Button = ({label}) => {

//   return (
// <button>{label}</button>

//   );
// }

// export default Button;

export default function Button({label}){
  return(
    <button>{label}</button>
  )
}